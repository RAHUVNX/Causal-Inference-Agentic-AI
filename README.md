# Causal Lift Web Platform

A full-stack web application for computing incremental lift using a two-path causal inference framework. This platform replicates a Spark-based causal lift system in a modern web architecture with React, Node.js, Express, PostgreSQL, and Prisma.

## Causal Framework

The platform implements a **two-path counterfactual simulation**:

```
Path A: Suggestion → Action
Path B: Action → Outcome (TRX / NBRX)
```

**Incremental lift** is computed as:

```
incremental_action  = predicted_action  − counterfactual_action
incremental_outcome = predicted_outcome − counterfactual_outcome
```

The counterfactual is generated by **zeroing out suggestion features** (setting suggestion_count, percent_other_hcps_suggested, and total_suggestions to zero) and re-predicting through both paths.

## Architecture

```
Root
├── client/                  # React + TypeScript frontend
│   ├── src/
│   │   ├── pages/           # Upload, Train, Lift, Dashboard, Metrics
│   │   ├── components/      # Sidebar, KpiCard, LoadingSpinner
│   │   ├── charts/          # Recharts: Incremental, Observed vs Predicted, Feature Importance
│   │   └── services/        # Axios API layer
│   ├── Dockerfile
│   └── nginx.conf
├── server/                  # Node.js + Express + TypeScript backend
│   ├── src/
│   │   ├── controllers/     # upload, train, lift, summary, featureImportance
│   │   ├── services/
│   │   │   ├── featureEngineer/   # Filter triangle, adstock, encoding, scaling
│   │   │   ├── models/            # PathAActionModel, PathBOutcomeModel (OLS regression)
│   │   │   ├── counterfactual/    # Zero-out simulation engine
│   │   │   ├── refinery/          # Pipeline orchestrator
│   │   │   └── metrics/           # R², MAE, RMSE, Accuracy, Precision, Recall, F1
│   │   ├── routes/
│   │   └── utils/           # Types, validation (Zod), CSV parser, linear algebra
│   ├── prisma/
│   │   └── schema.prisma    # PostgreSQL schema
│   └── Dockerfile
├── docker-compose.yml
└── README.md
```

## Backend Service Layers

| Layer | Module | Responsibility |
|---|---|---|
| **featureEngineer** | `services/featureEngineer/` | Filter triangle, study period filtering, percent_other_hcps_suggested, adstock transformation, one-hot encoding, z-score standardization |
| **models** | `services/models/` | PathAActionModel (Suggestion→Action), PathBOutcomeModel (Action→TRX/NBRX). OLS linear regression via normal equation with ridge regularization |
| **counterfactual** | `services/counterfactual/` | Zero out suggestion features, generate counterfactual_action, substitute into outcome model, compute counterfactual_outcome |
| **refinery** | `services/refinery/` | Orchestrate full pipeline: feature engineering → train Path A → train Path B → counterfactual simulation → aggregate monthly summaries |
| **metrics** | `services/metrics/` | R², MAE, RMSE, plus binarized classification metrics (Accuracy, Precision, Recall, F1). Feature importance ranking by |coefficient| |

## API Endpoints

| Method | Endpoint | Description |
|---|---|---|
| `POST` | `/api/upload-data` | Upload CSV file, parse and store observations |
| `POST` | `/api/train-models` | Train Path A and Path B models |
| `POST` | `/api/run-lift` | Execute full counterfactual simulation |
| `GET` | `/api/summary` | Monthly aggregated results and KPIs |
| `GET` | `/api/feature-importance` | Ranked feature importance from trained models |
| `GET` | `/api/health` | Health check |

## Database Schema

- **observations** — Raw HCP-month observation records from CSV uploads
- **model_metadata** — Trained model coefficients, feature lists, scaler states
- **model_runs** — Training/simulation run history with status tracking
- **lift_results** — Per-HCP-month predicted, counterfactual, and incremental values
- **feature_importance** — Ranked feature importance from each trained model

## Setup

### Prerequisites

- Node.js 18+
- PostgreSQL 14+ (or Docker)

### Option 1: Docker Compose (Recommended)

```bash
docker-compose up --build
```

This starts:
- PostgreSQL on port 5432
- Backend API on port 4000
- Frontend on port 3000

Open http://localhost:3000 in your browser.

### Option 2: Local Development

**1. Start PostgreSQL** and create a database:

```bash
createdb causal_lift_db
```

**2. Backend setup:**

```bash
cd server
cp .env.example .env
# Edit .env with your DATABASE_URL
npm install
npx prisma migrate deploy
npx prisma generate
npm run dev
```

**3. Frontend setup:**

```bash
cd client
npm install
npm run dev
```

Open http://localhost:3000 in your browser.

## CSV Format

Upload a CSV with these columns:

| Column | Required | Type | Description |
|---|---|---|---|
| `hcp_id` | Yes | string | Healthcare professional identifier |
| `month` | Yes | date | Observation month (YYYY-MM-DD) |
| `suggestion_type` | Yes | string | Type of suggestion (e.g., "email") |
| `action_type` | Yes | string | Type of action (e.g., "detail") |
| `outcome_type` | Yes | string | "trx" or "nbrx" |
| `suggestion_count` | Yes | number | Number of suggestions |
| `action_count` | Yes | number | Number of actions taken |
| `outcome_count` | Yes | number | TRX or NBRX value |
| `specialty_code` | No | string | HCP specialty |
| `region_code` | No | string | Geographic region |
| `tenure_months` | No | number | HCP tenure in months |
| `prior_trx` | No | number | Prior period TRX |
| `prior_nbrx` | No | number | Prior period NBRX |
| `total_suggestions` | No | number | Total suggestions received |
| `theme` | No | string | Suggestion theme for theme-level analysis |

## Usage Flow

1. **Upload Data** — Upload your CSV observation data
2. **Train Models** — Configure filter triangle and train Path A + Path B
3. **Run Lift Simulation** — Execute counterfactual simulation and view incremental lift
4. **Dashboard** — View KPIs, charts, and monthly summaries
5. **Model Metrics** — Inspect R², MAE, feature importance rankings

## Tech Stack

- **Frontend:** React 18, TypeScript, Recharts, Axios, Vite
- **Backend:** Node.js, Express, TypeScript, Prisma ORM, Zod validation
- **Database:** PostgreSQL
- **Infrastructure:** Docker, Docker Compose, Nginx
